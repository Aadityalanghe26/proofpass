/**
 * Real SDK wrapper — used when VITE_CONTRACT_ADDRESS is set.
 * Aliased to midnight-sdk.stub.ts in dev/simulation mode by vite.config.ts.
 */
export { Contract, createMidnightProvider } from '@midnight-ntwrk/midnight-js-contracts';
